// Jest (and some tooling) expects babel config under this conventional name.
// We delegate to the existing `config.babel` to avoid duplicating configuration.
module.exports = require('./config.babel');

